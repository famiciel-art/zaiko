{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid && (auth.uid === 'FSMueAKryHa4xZse0RmzeqcNNco1' || auth.token.email === 'famiciel@gmail.com')",
        ".write": "auth != null && auth.uid === $uid && (auth.uid === 'FSMueAKryHa4xZse0RmzeqcNNco1' || auth.token.email === 'famiciel@gmail.com')"
      }
    }
  }
}
